package com.locklab.pairmatch.repository;

import com.locklab.pairmatch.entity.PairGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PairGroupRepository extends JpaRepository<PairGroup, Long> {
}
